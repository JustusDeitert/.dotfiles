#!/bin/sh

# define global node modules to be installed
node_modules=(
    webpack
    grunt-cli
    gulp
    bower
    yo
    generator-webapp
    node-sass
)

# install specified system brews
for node_module in "${node_modules[@]}"
    do npm install -g $node_module
done
